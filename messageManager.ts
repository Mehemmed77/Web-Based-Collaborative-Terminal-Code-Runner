import { rooms } from "./server.ts";
import { v4 as id } from "uuid";

export default function messageManager(msg: string) {
    switch (msg) {
        case "JOIN_ROOM":


            break;
        case "CREATE_ROOM":
            

            break;
    }
}